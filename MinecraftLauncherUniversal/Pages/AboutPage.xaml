<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MinecraftLauncherUniversal.Pages.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MinecraftLauncherUniversal.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" NavigationCacheMode="Disabled"
    xmlns:labs="using:CommunityToolkit.Labs.WinUI"
    Background="Transparent">

    <Grid Margin="58, 42, 58,0" CanBeScrollAnchor="True" RowSpacing="2">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Expander HorizontalAlignment="Stretch" Grid.Row="1" HorizontalContentAlignment="Stretch" IsExpanded="True">
            <Expander.Header>
                <Grid Margin="8" x:Name="AboutAppGrid" Padding="16,16,4,16">
                    <StackPanel HorizontalAlignment="Left" VerticalAlignment="Center" Orientation="Horizontal">
                        <StackPanel Orientation="Horizontal">
                            <ImageIcon Source="/Assets/minecraft-icon.jpg" Height="30" Width="30" Margin="0,0,8,0"/>
                        </StackPanel>
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="MinecraftLauncher" FontSize="15"/>
                            <TextBlock Text="by Jurij15" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>
                    </StackPanel>
                    <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock FontWeight="Normal" FontSize="13" x:Name="VersionBox" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>
                </Grid>
            </Expander.Header>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" HorizontalAlignment="Right">
                    <TextBlock Text="Open source libraries used:" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    <HyperlinkButton NavigateUri="https://github.com/CmlLib/CmlLib.Core" Content="CmlLib.Core"/>
                </StackPanel>
                <StackPanel Grid.Row="0" HorizontalAlignment="Left">
                    <HyperlinkButton NavigateUri="https://github.com/Jurij15/MinecraftLauncher" Content="GitHub"/>
                </StackPanel>
            </Grid>
        </Expander>
        <Expander HorizontalAlignment="Stretch" Grid.Row="2" HorizontalContentAlignment="Stretch" Margin="0,4,0,0" Visibility="Collapsed">
            <Expander.Header>
                <Grid Margin="0" CornerRadius="8"  Padding="8" Height="{Binding ElementName=AboutAppGrid, Path=ActualHeight}" Width="{Binding ElementName=AboutAppGrid, Path=ActualWidth}">
                    <StackPanel HorizontalAlignment="Left" VerticalAlignment="Center" Orientation="Horizontal">
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="Patch Notes" FontSize="15"/>
                            <TextBlock Text="{Binding ElementName=VersionBox, Path=Text}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Expander.Header>
            <StackPanel>
                <TextBlock>
                    This is the second version of the launcher, based on WinUI 3 and WindowsAppSDK
                </TextBlock>
            </StackPanel>
        </Expander>
        <labs:SettingsCard Header="Updates" Description="Check for updates" 
              Grid.Row="3">
            <labs:SettingsCard.HeaderIcon>
                <FontIcon Glyph="&#xE777;"/>
            </labs:SettingsCard.HeaderIcon>
            <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Style="{ThemeResource AccentButtonStyle}" Content="Check For Updates" x:Name="CheckForUpdatesBtn" Click="CheckForUpdatesBtn_Click">
                    <Button.Flyout>
                        <Flyout x:Name="UpdateFlyout">
                            <StackPanel Orientation="Horizontal">
                                <FontIcon Glyph="&#xE777;" x:Name="UpdateIcon" Visibility="Collapsed"/>
                                <FontIcon Glyph="&#xE8D8;" x:Name="CannotUpdateIcon" Visibility="Collapsed"/>
                                <FontIcon Glyph="&#xE73E;" x:Name="NoUpdatesIcon" Visibility="Collapsed"/>
                                <StackPanel Margin="4,0,0,0">
                                    <TextBlock x:Name="StatusBox" Style="{ThemeResource BaseTextBlockStyle}"/>
                                    <TextBlock x:Name="AditionalStuffBox" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                </StackPanel>
                            </StackPanel>
                        </Flyout>
                    </Button.Flyout>
                </Button>
            </StackPanel>
        </labs:SettingsCard>
    </Grid>
</Page>
