<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MinecraftLauncherUniversal.Pages.ForgePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MinecraftLauncherUniversal.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:MinecraftLauncherUniversal.Controls"
    
    NavigationCacheMode="Required"
    
    mc:Ignorable="d">

    <Grid Margin="58, 42, 58,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <AutoSuggestBox x:Name="SearchBox" Visibility="Collapsed" Grid.Row="0"/>
        <InfoBar x:Name="MinimumVersionInfoBar" Message="Only Forge versions above 1.7.10 are supported!" IsOpen="True" IsClosable="False" Grid.Row="1"/>
        <GridView x:Name="ForgeVersionsPanel" Grid.Row="2" Margin="4" Loaded="ForgeVersionsPanel_Loaded" SelectionMode="None">
            
        </GridView>
    </Grid>
</Page>
