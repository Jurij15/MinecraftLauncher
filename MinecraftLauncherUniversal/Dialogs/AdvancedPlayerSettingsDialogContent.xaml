<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MinecraftLauncherUniversal.Dialogs.AdvancedPlayerSettingsDialogContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MinecraftLauncherUniversal.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" Unloaded="Page_Unloaded"
    mc:Ignorable="d">

    <Grid>
        <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <InfoBar Message="Only change these settings if you know what you are doing!" Severity="Warning" Margin="0,0,0,4" IsOpen="True" IsClosable="False"/>
            <TextBox x:Name="CustomUUIDBox" PlaceholderText="UUID" Header="Custom UUID" Margin="0,0,0,4"/>
            <TextBox x:Name="AccessTokenBox" PlaceholderText="Access Token" Header="Access Token" Margin="0,0,0,4"/>
        </StackPanel>
    </Grid>
</Page>
